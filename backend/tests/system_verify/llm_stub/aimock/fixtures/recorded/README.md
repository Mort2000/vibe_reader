# Recorded fixtures

Place manually reviewed, desensitized fixtures here after explicit record sessions.

CI must not use `--record` by default. Recorded prompts may contain copyrighted text — only commit fixtures that comply with corpus authorization.
